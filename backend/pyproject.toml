[project]
name = "ai-document-router-backend"
version = "0.1.0"
description = "AI Document Router backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic-settings>=2.6",
    "sqlmodel>=0.0.39",
    "alembic>=1.19.1",
    "cryptography>=50.0.0",
    "webdav4>=0.11.0",
    "pypdf>=6.16.1",
    "apscheduler>=3.11.3",
    # services/ai.py and services/webdav.py import httpx directly. It resolves transitively
    # through webdav4 anyway, but a production install (`uv sync --no-dev`) would break the
    # moment that transitive edge changed, so declare what we actually import.
    "httpx>=0.28",
    "pypdfium2>=5.13.0",
    "pillow>=12.3.0",
]

[dependency-groups]
dev = [
    "ruff>=0.8",
    "pytest>=8.3",
    "mypy>=1.13",
    "httpx>=0.27",
    "respx>=0.23.1",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "."
plugins = ["pydantic.mypy"]

# webdav4 ships no py.typed marker. Confined to services/webdav.py, which is the only
# module allowed to import it, so the untyped surface stays behind our own dataclasses.
[[tool.mypy.overrides]]
module = ["webdav4.*", "apscheduler.*", "pypdfium2.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
